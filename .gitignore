# Page scans are kept out of git while the rights position is unconfirmed.
# See RIGHTS.md. Remove these lines once that is settled.
source/pages/*.jpg
source/pages/*.jpeg
source/pages/*.png
source/pages/*.tif
source/pages/*.tiff
!source/pages/README.md

# Model drafts are regenerable and must not be mistaken for verified content.
content/drafts/*.json

.DS_Store
__pycache__/
*.pyc
